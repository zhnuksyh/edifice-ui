{
  "extends": "./config/tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["react", "node"]
  },
  "include": [
    "tokens",
    "presets",
    "utils",
    "hooks",
    "components",
    "config/tailwind.base.ts",
    "nativewind-env.d.ts"
  ],
  "exclude": ["node_modules", "dist", "build", ".next", "mcp"]
}
