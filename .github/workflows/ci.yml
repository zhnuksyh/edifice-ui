name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  library:
    name: Library typecheck + tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dev dependencies
        run: npm ci

      - name: Typecheck (tokens, utils, hooks, components, config)
        run: npm run typecheck

      - name: Typecheck test files (RTL + jest-dom types)
        run: npm run typecheck:test

      - name: Tests (utils, hooks, and all web components)
        run: npm test

  mcp:
    name: MCP server build + smoke
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mcp
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mcp/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Smoke test (stdio client end-to-end)
        run: npm run smoke
