/// <reference types="nativewind/types" />

// NativeWind augments React Native's core components (View, Text, Pressable,
// TextInput, etc.) to accept a `className` prop. Consuming mobile projects must
// include this reference (or their own `nativewind-env.d.ts`) so the `.native`
// components in this library typecheck.
